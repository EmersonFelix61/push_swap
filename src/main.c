#include "../includes/push_swap.h"

int	main(int argc, char **argv)
{
	int	*numbers;
	
	if (argc == 1)
		return (0);
	numbers = malloc(sizeof(char) * (argc - 1));
	if (!numbers)
		return (1);
	if (!)
}
